//8.定义一个整数数组，包含元素[1,3,4,7,8]，获取数组中数组角标和对应的元素都为偶数的元素与个数，并在控制台打印输出。


//声明数组
var arr = [1, 3, 4, 7, 8]
//遍历
for (var i = 0; i < arr.length; i++) {
    //判断角标与元素是否为偶数
    if (i % 2 == 0 && arr[i] % 2 == 0) {
        //如果都为偶数则打印
        console.log(arr[i])
    }
}