// 3.求2000-3000年之间的所有的闰年以及个数
//定义一个变量 用于计算有多少个闰年
var count = 0;
//循环2000-3000
for (var i = 2000; i <= 3000; i++) {
  // 判断是否是闰年
  if ((i % 4 === 0 && i % 100 != 0) || i % 400 === 0) {
    //如果是闰年  则加一
    count++;
    //输出所有闰年
    console.log(i);
  }
}
//输出闰年个数
console.log("共有" + count + "闰年");
